import torch



def element_wise_multiplication(a: torch.Tensor, b: torch.Tensor) -> torch.Tensor:

    assert isinstance(a, torch.Tensor) and isinstance(b, torch.Tensor)

    # =========================================

    # insert your code here
    c = None

    # =========================================

    return c
    

def batch_transposed_matrix_multiplication(a: torch.Tensor, b: torch.Tensor) -> torch.Tensor:

    assert isinstance(a, torch.Tensor) and isinstance(b, torch.Tensor)

    # =========================================

    # insert your code here
    c = None

    # =========================================

    return c

def aggregate_4d_tensor_through_3rd_dim(a: torch.Tensor) -> torch.Tensor:

    # =========================================

    # insert your code here
    c = None

    # =========================================

    return c
